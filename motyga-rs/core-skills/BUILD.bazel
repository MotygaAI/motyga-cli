load("//:defs.bzl", "motyga_rust_crate")

motyga_rust_crate(
    name = "core-skills",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ),
    crate_name = "motyga_core_skills",
)
