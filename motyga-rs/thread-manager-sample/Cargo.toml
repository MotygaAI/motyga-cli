[package]
name = "motyga-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single Motyga workspace dependency.
# Add new Motyga surface area to `motyga-core-api` instead of depending on
# additional `motyga-*` crates here.
motyga-core-api = { workspace = true }
tracing = { workspace = true }
