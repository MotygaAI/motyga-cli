load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "motyga_rust_crate")
load("//bazel/platforms:release_binaries.bzl", "multiplatform_binaries")

motyga_rust_crate(
    name = "cli",
    crate_name = "motyga_cli",
    extra_binaries = [
        "//motyga-rs/bwrap:bwrap",
    ],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
)

multiplatform_binaries(
    name = "motyga",
)
