load("//:defs.bzl", "motyga_rust_crate")

motyga_rust_crate(
    name = "exec",
    crate_name = "motyga_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)
